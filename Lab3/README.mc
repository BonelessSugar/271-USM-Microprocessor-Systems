lab3.docx is the written lab report file
lab3.pdf is the given lab instructions
main given.c is the given main.c file from the instructor to edit
main morse.c is the main.c file that runs the SOS LED blink
main toggle LED.c is the main.c file that toggles an LED and configures the pins to do so
